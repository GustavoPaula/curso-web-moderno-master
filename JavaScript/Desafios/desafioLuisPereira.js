const listaDeListaInteiros = [
    [1,2,3,3,2,1]
]

const lista2 = [1,2,3,3,2,1]

listaDeListaInteiros.filter(lista => {
    lista.filter(numeroLista, index => {
        lista2.filter(numeroLista2 => {
            numeroLista === numeroLista2 ? numeroLista : -1
            return 
        })

    })
})
